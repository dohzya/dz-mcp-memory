# Configuration example for MCP dz-memory
# Copy this file to .env and adjust the values as needed

# Server configuration
PORT=8000
LOG_LEVEL=INFO

# Authentication
MCP_AUTH_TOKEN=your-secure-token-here

# Database configuration
# Options: "memory", "sqlite", "pgvector"
DATABASE_TYPE=memory

# SQLite configuration (when DATABASE_TYPE=sqlite)
DATABASE_PATH=./memory.db

# PostgreSQL configuration (when DATABASE_TYPE=pgvector)
DATABASE_URL=postgresql://username:password@localhost:5432/memory_db

# Memory service configuration
CHUNK_SIZE=500
MAX_MEMORIES=10000

# Optional: External services (future features)
# OPENAI_API_KEY=your-openai-key-for-embeddings
# EMBEDDING_MODEL=text-embedding-ada-002
